#!/bin/bash
# 金鑰守門：提交前掃描，發現疑似金鑰就擋下。
# 啟用：git config core.hooksPath .githooks
python3 "$(git rev-parse --show-toplevel)/scripts/scan_secrets.py" --staged || {
  echo ""
  echo "⛔ 提交已中止：偵測到疑似金鑰。修正後再提交，或確認為佔位符後用 --no-verify 略過。"
  exit 1
}
